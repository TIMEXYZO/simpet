{
  "name": "render-node-app",
  "version": "1.0.0",
  "description": "A simple Node.js app deployed on Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "rm -rf node_modules && npm install"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
